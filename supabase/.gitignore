# Supabase
.branches
.temp
# Local service configuration is intentionally tracked so CI can run an
# isolated Supabase stack with the same migrations and seed data.

# dotenvx
.env.keys
.env.local
.env.*.local
